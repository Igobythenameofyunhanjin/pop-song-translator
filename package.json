{
    "name": "hiphop-translator-monorepo",
    "private": true,
    "scripts": {
      "build": "cd frontend && npm install && npm run build && cd .. && cd backend && npm install",
      "start": "cd backend && node index.cjs"
    }
  }
  